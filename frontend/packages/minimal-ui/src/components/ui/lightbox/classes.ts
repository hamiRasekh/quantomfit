import { createClasses } from '@/ui/theme/create-classes';

// ----------------------------------------------------------------------

export const lightboxClasses = {
  root: createClasses('lightbox__root'),
};
